#!/usr/bin/python
import sys

def integrate (f, range):
    total=0.0

for x in range(len(rangex)-1):
    s=(rangex[x+1]-range[x])/0.5
    f(a)=float(rangex[x]
    f(b)=float(range[x+1])
    height=f(float(fa+fb))
    total += s*height

return total
