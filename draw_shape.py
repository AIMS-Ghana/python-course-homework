#!/usr/bin/python3
import sys
import turtle
import shapes
grant=turtle.Turtle()
def draw(name, area):
	if name== "TRIANGLE":
		#length= shapes.value
		#draw triangle
		#canvas.setup(400,200)
		grant = Turtle()
		grant.forward(side)  # make grant draw a square
		grant.left(120)
		grant.forward(side)
		grant.left(120)
		grant.forward(side)

		#grant.exitonclick()	

	if name =="SQUARE":
		#draw square
		#canvas.setup(400,200)
		grant = Turtle()
		grant.forward(side)         
		grant.left(90)
		grant.forward(side)
		grant.left(90)
		grant.forward(side)
		grant.left(90)
		grant.forward(side)
              	
		#grant.exitonclick()	
	
	if name =="CIRCLE":
		#draw cicrcle
		#canvas = Screen()
		#canvas.setup(400,200)
		grant = Turtle()
		grant.forward(side)         
turtle.done		
              	
		#grant.exitonclick()	
	
#if __name__ == "__main__":
#	draw("TRIANGLE",100)
#	draw("SQUARE",100)
	
