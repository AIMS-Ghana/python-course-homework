#!/usr/bin/python
import math
import sys

def root(bisectf, x):
	#tol=1e-5
	a==rangex
	b==rangex-1
	while (b-a)>0.001:
	#for ab in range(a,b)
	    c = (a+b)/2.0
	    if f(c) == 0:
	        return c
	    elif f(a)*f(c) < 0:
	            root(bisectf, x)
	    else :
	           return (bisectf, x)

