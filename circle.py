#!/usr/bin/python

import maths
values="area={O}/n perimeter={1}"
area ""
perimeter ""
import sys
  assert(r>0) means the radius of the circle
if len(sys.argv)==2 "not possible"
return res
area=pi*(r)**2
perimeter=2*pi*r)
   print(value.format(str(area),str(perimeter)))
